# 忆程 ProjectMemo｜复赛配图 AI 生成提示词

> 用途：为《复赛作品说明文档》生成投稿级配图，覆盖草案第 4 章图 4～图 6（当前为 Mermaid 源码）与宣传海报。
> 配色：沿用产品 Design Token（`harmonyos/entry/src/main/resources/base/element/color.json`、`DesignTokens.ets`）。
> 图内文字：以中文标签为主，允许少量短句；英文提示词只用于驱动模型，图内渲染文字为简体中文。
> 生成工具：NanoBanana / Gemini 2.x 图像、GPT-Image / DALL·E、Midjourney（海报）。
> 更新日期：2026-09-15

## 统一色板（复制到每个提示词末尾已生效）

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 主色 Deep Navy | `#142338` | 核心模块边框、区域标签、正文文字 |
| 辅色 Cyan Accent | `#0E7490` | 关键路径、结果与输出模块、状态图 CURRENT |
| 辅色浅底 Cyan Wash | `#EDF8F9` | 关键路径所在分组的极浅背景 |
| 中性 Slate | `#475B70` / `#788C9E` / `#9EAEBE` | 次要边框、箭头、注释文字 |
| 语义色 Amber | `#D97706`（浅底 `#FEF7EE`） | 回退、待验证、限频标记（极少量） |
| 语义色 Vermillion | `#DC2626`（浅底 `#FEF2F2`） | 冲突、阻断、失败标记（极少量） |
| 模块填充 | `#FFFFFF` | 所有内容框 |
| 分组背景 | `#F4F7FA` | 大区域面板 |

硬约束：整图只用 Navy + Cyan 两种色相，Amber 与 Vermillion 仅作语义标记；≥70% 面积为白色或近白；模块一律白底 + 细边框，禁止彩色填充、渐变、阴影堆叠和彩虹配色；黑白打印后信息仍完整。

---

## 图 6.1 — 端云分层架构与数据流（对应草案图 6）

适用类型：system overview / architecture
推荐尺寸：16:9（1792×1024），A4 横向独占半页或整页

```
A highly detailed, information-dense academic system-architecture diagram in the style of
top-tier systems-conference whitepapers, rendered as a clean three-tier horizontal layout
read left to right: DEVICE TIER, SERVICE TIER, PERSISTENCE TIER, with a fourth narrow column
on the far right for an OPTIONAL CHANNEL adapter. All in-text labels are Simplified Chinese,
short (2 to 8 characters), set in a neutral sans-serif; no decorative imagery, no 3D, no
skeuomorphic icons, flat vector style only.

=== SECTION: 鸿蒙端（leftmost, largest panel, faint grey #F4F7FA background, small-caps
Deep Navy #142338 label "鸿蒙端 · ArkTS / ArkUI" with a thin grey divider） ===
Inside, five white rounded-rectangle boxes with Deep Navy borders, stacked in two columns:
1) "客户端页面" containing a nested list of seven small grey-bordered chips:
   项目 / 工作台 / 行动板 / 记忆时间线 / 介入详情 / 登录 / 指引;
2) "系统通知" containing a small monochrome notification-banner thumbnail and the caption
   "发布 + 深链回到提醒";
3) "附件录入" containing two tiny monochrome picker thumbnails (a photo grid, a PDF page)
   and the caption "图片 / 文档选择器";
4) "本地状态" containing a small monochrome key-value table thumbnail and the caption
   "会话 · 草稿 · 标识";
5) "布局适配" containing three tiny monochrome device-outline glyphs side by side labelled
   "phone / tablet / 2in1" plus a light-and-dark swatch pair labelled "深色资源" and an
   accessibility glyph labelled "无障碍标签".
A dashed grey self-contained note box under panel 4 and 5 reading "仅本地状态，非业务事实"
connected to panel 4 with a dotted arrow.

=== SECTION: 服务端（center panel, faint grey #F4F7FA background, small-caps Deep Navy label
"服务端 · Next.js Route Handlers"） ===
A vertical chain of five white boxes, each with a Deep Navy border except the trust-related
one which uses Cyan #0E7490:
1) "入口校验" with a tiny monochrome JSON-schema thumbnail and caption "Zod 参数校验";
2) "鉴权与归属" (Cyan border) containing two stacked sub-chips "会话鉴权" and
   "项目成员校验", plus a small monochrome lock-and-scope glyph and the caption
   "越权按不存在处理";
3) "用例编排" containing three parallel thin sub-boxes "services" / "repositories" /
   "事务边界" with a tiny monochrome transaction-bracket thumbnail;
4) "Agent 管线" containing a two-branch decision: upper branch "真实模型" leading to a
   small monochrome JSON-validation thumbnail, lower branch "确定性规则" in grey; both
   branches converge into a Cyan-bordered box "结构化结果 + 引用", and a dashed Amber
   #D97706 arrow loops from the model branch down to a small box "回退并记录原因";
5) "证据与审计" containing three tiny monochrome thumbnails: a citation-bracket list, a
   snapshot-stack, and a ledger table, captioned "逐句来源" / "不可变快照" / "运行记录".

=== SECTION: 持久层（right panel, faint grey #F4F7FA background, small-caps Deep Navy label
"持久层 · Prisma + SQLite"） ===
Three white boxes: "业务事实" with a small monochrome entity-relationship grid thumbnail and
the caption "29 模型 / 17 迁移"; "附件存储" with a tiny monochrome file-plus-hash-chain
thumbnail and caption "sha256 修订链"; "审计与回执" with a tiny monochrome append-only log
thumbnail and caption "同事务提交".

=== SECTION: 可选通道（narrow far-right column, light warm-grey panel, small-caps label
"小艺适配层"） ===
One white box with a dashed Amber #D97706 border, containing a tiny monochrome workflow-node
thumbnail and two stacked captions "默认关闭" and "平台联调待验证"; a dashed grey arrow from
this box into "用例编排".

=== CONNECTIONS ===
A single thick Cyan #0E7490 arrow from the DEVICE panel to "入口校验" labelled
"Bearer Token · REST"; thin Dark-grey arrows "入口校验 → 鉴权与归属 → 用例编排";
two thin arrows from "用例编排" into the PERSISTENCE boxes labelled "业务写入" and
"回执"; one thin arrow from "用例编排" up into "Agent 管线" labelled "模型调用在事务外";
one thin arrow returning from "证据与审计" to the DEVICE "系统通知" box labelled
"通知意图"; one dashed grey feedback arrow from "持久层" back to the DEVICE panel
labelled "状态刷新与新鲜度".

=== STYLE SPECIFICATIONS ===
Flat vector academic diagram, no gradient, no drop shadow, no bevel, no photorealism, no
decorative background texture. White #FFFFFF module fills with 1px borders only. Two color
hues in the entire figure: Deep Navy #142338 for structure and Cyan #0E7490 for the trusted
data path; Amber #D97706 and Vermillion #DC2626 appear only as tiny semantic markers.
Region panels use faint grey #F4F7FA. Body text Deep Navy #142338, secondary annotations
Medium Grey #788C9E, arrows #475B70. Section labels in small caps with a thin grey rule,
never as colored banner bars. At least 70 percent of the canvas is white or near-white.
All module boxes contain sub-content; no empty placeholder boxes. The figure must remain
fully readable when printed in pure black and white. Aspect ratio 16:9, high detail, sharp
anti-aliased text, generous margins, balanced optical alignment, no watermark, no logo.
```

中文标注版（用于只吃中文提示词的工具）：

> 画一张顶会系统架构图风格的扁平矢量示意图，16:9，横向三栏加右侧窄栏：鸿蒙端 / 服务端 / 持久层 / 小艺适配层。全部图内文字用简体中文短标签（2–8 字），无插画、无 3D、无渐变、无阴影。
> 鸿蒙端栏：五个白底细边圆角框——「客户端页面」（内含项目/工作台/行动板/记忆时间线/介入详情/登录/指引七个小标签）、「系统通知」（通知条缩略图 + “发布 + 深链回到提醒”）、「附件录入」（图片九宫格与 PDF 页两个灰度缩略图 + “图片/文档选择器”）、「本地状态」（键值表缩略图 + “会话·草稿·标识”）、「布局适配」（phone/tablet/2in1 三个设备轮廓 + 深浅色卡 + 无障碍标签）；底部虚线框注明“仅本地状态，非业务事实”。
> 服务端栏：自上而下五个白框——「入口校验」（Zod）、「鉴权与归属」（青色边框，含“会话鉴权”“项目成员校验”“越权按不存在处理”）、「用例编排」（services / repositories / 事务边界）、「Agent 管线」（真实模型与确定性规则两支汇合为“结构化结果 + 引用”，模型支有琥珀色虚线回退箭头标注“回退并记录原因”）、「证据与审计」（逐句来源 / 不可变快照 / 运行记录三个灰度缩略图）。
> 持久层栏：「业务事实」（实体关系网格 + “29 模型 / 17 迁移”）、「附件存储」（文件加哈希链 + “sha256 修订链”）、「审计与回执」（追加日志 + “同事务提交”）。
> 右侧窄栏：「小艺适配层」用琥珀色虚线边框，标注“默认关闭”“平台联调待验证”，虚线箭头连到用例编排。
> 连线：一条青色粗箭头从端侧指向入口校验，标注“Bearer Token · REST”；一条虚线灰箭头从持久层回到端侧标注“状态刷新与新鲜度”；一条细箭头从用例编排指向 Agent 管线标注“模型调用在事务外”。
> 配色：整图只用深藏青 #142338 与青 #0E7490 两个色相，琥珀 #D97706 与朱红 #DC2626 仅作极少量语义标记；模块一律白底 + 1px 细边框，分区背景浅灰 #F4F7FA；区域标题用小号大写字距标签 + 细灰分割线，禁止彩色横幅；画面 ≥70% 为白或近白；黑白打印仍完整可读。

必须出现：三层分区标题、七个页面标签、鉴权与归属框、Agent 双分支与回退标注、小艺“默认关闭/待验证”、29 模型 17 迁移、模型调用在事务外。
禁止出现：把端侧画成含数据库、写“完全离线”、写“已接入小艺”、彩色填充大色块、渐变或玻璃拟态、任何真实域名/IP/密钥、华为或小艺官方 logo。

---

## 图 6.2 — 记忆时态生命周期状态机（对应草案图 5）

适用类型：state machine / flowchart
推荐尺寸：3:2（1536×1024）

```
A precise academic state-transition diagram for a temporal memory model, flat vector style,
landscape 3:2, four principal states arranged on a soft diamond layout with one absorbing
side-state, all labels in Simplified Chinese with the English state code shown beneath each
Chinese name in small monospace caps.

=== STATES (each a white rounded rectangle, 1px colored border, generous internal padding) ===
1) "未知" / UNKNOWN — Medium Grey #9EAEBE border, containing a tiny monochrome
   question-mark-plus-evidence-card thumbnail and the caption "证据不足";
2) "当前有效" / CURRENT — Cyan #0E7490 border, containing a small monochrome card thumbnail
   with a check glyph and caption "参与当前结论";
3) "已被取代" / SUPERSEDED — Deep Navy #142338 border drawn with a dashed outline, containing
   a small monochrome thumbnail of two overlapping cards with the older one faded, caption
   "保留历史，不再参与";
4) "存在争议" / CONTESTED — Vermillion #DC2626 border, containing a tiny monochrome thumbnail
   of two opposing arrows over a card, caption "相互矛盾的来源";
5) a small side panel at bottom-right titled "归档" containing two stacked grey chips
   "移出当前视图" and "不改变事实有效性".

=== TRANSITIONS (thin directed arrows, each with a short Chinese label) ===
start-dot to UNKNOWN labeled "新卡片";
UNKNOWN to CURRENT labeled "用户确认来源";
UNKNOWN self-loop labeled "保持无法判断，不折算为不成立";
CURRENT to SUPERSEDED labeled "新决策或新证据取代";
CURRENT to CONTESTED labeled "出现矛盾来源";
CONTESTED to CURRENT labeled "用户裁决保留其一";
CONTESTED to UNKNOWN labeled "证据仍然不足";
SUPERSEDED to CURRENT labeled "撤销取代关系";
CURRENT and SUPERSEDED each connected to the 归档 side panel with dotted grey arrows.

=== GLOBAL ANNOTATIONS ===
A persistent legend strip along the bottom with three swatches: a solid cyan-edged box
captioned "参与结论", a dashed navy-edged box captioned "仅历史可查", a red-edged box
captioned "需人工裁决"; plus one footnote line in Medium Grey reading
"每条状态保留原因码与证据引用；人工确认只确认来源声明，不证明客观事实".

=== STYLE SPECIFICATIONS ===
Flat vector, no gradient, no shadow, no 3D, no decorative background. White fills with 1px
borders only; two hues (Deep Navy #142338 structure, Cyan #0E7490 for the trusted current
state) plus minimal Amber #D97706 and Vermillion #DC2626 markers only where semantically
required. Arrowheads small and consistent; crossing minimized; body text #142338, secondary
text #788C9E. At least 70 percent white canvas. Readable in pure black and white print.
Sharp anti-aliased Chinese glyphs, no watermark, no logo.
```

中文标注版：

> 画一张学术风格状态机图，3:2，扁平矢量，图内文字简体中文 + 英文状态码（UNKNOWN / CURRENT / SUPERSEDED / CONTESTED）。四个主状态 + 右下「归档」侧栏：未知（灰边，含“证据不足”缩略图）、当前有效（青 #0E7490 边，含勾选卡片缩略图 + “参与当前结论”）、已被取代（藏青 #142338 虚线边，含新旧两张叠放卡片、旧卡淡化 + “保留历史，不再参与”）、存在争议（朱红 #DC2626 边，含两个相向箭头 + “相互矛盾的来源”）。
> 转移箭头均带中文短标签：起始点→未知“新卡片”；未知→当前有效“用户确认来源”；未知自环“保持无法判断，不折算为不成立”；当前有效→已被取代“新决策或新证据取代”；当前有效→存在争议“出现矛盾来源”；存在争议→当前有效“用户裁决保留其一”；存在争议→未知“证据仍然不足”；已被取代→当前有效“撤销取代关系”；当前有效与已被取代各一条虚线连到归档侧栏。
> 底部图例三条：青实边“参与结论”、藏青虚边“仅历史可查”、红边“需人工裁决”；脚注一行：“每条状态保留原因码与证据引用；人工确认只确认来源声明，不证明客观事实”。
> 配色只用藏青 + 青两色相，朱红仅用于争议；白底细边框，无渐变无阴影，≥70% 留白，黑白打印可读。

必须出现：四态英文名、UNKNOWN 自环、SUPERSEDED 可撤销、归档与状态分离、底部图例与脚注。
禁止出现：把 UNKNOWN 画成“失败/错误”、把归档画成删除、出现“AI 自动判定为真”一类表述、彩色填充大色块。

---

## 图 6.3 — 主动介入到结果回写闭环（对应草案图 4）

适用类型：pipeline / closed-loop flow
推荐尺寸：16:9（1792×1024）

```
A highly detailed academic closed-loop pipeline diagram, flat vector, landscape 16:9,
arranged as six stages in a clockwise loop returning to the first stage, each stage a white
rounded rectangle with a small-caps Deep Navy label and a thin grey rule, every stage
internally populated with two or three sub-chips and one monochrome thumbnail, no empty boxes.

=== STAGE 1: 触发检测 ===
Sub-chips "截止逼近" / "风险未处理" / "项目停滞" / "材料缺口"; a tiny monochrome calendar-and-
gauge thumbnail; a small Amber #D97706 marker chip "每日预算 3 次 · 免打扰 23:00–08:00".

=== STAGE 2: 证据组织 ===
Sub-chips "引用卡片" / "附件修订" / "触发原因"; a tiny monochrome citation-list thumbnail with
three bracketed reference markers.

=== STAGE 3: 用户决策 ===
Three side-by-side outcome chips: "接受" (Cyan #0E7490 border), "稍后" (grey border),
"忽略" (grey border); a small monochrome mobile notification-card thumbnail above them;
footnote "系统不代替用户决定".

=== STAGE 4: 行动生成 ===
Sub-chips "标题 / 优先级 / 截止" / "依赖由当前数据评估" / "幂等去重键"; a tiny monochrome
dependency-graph thumbnail with three nodes and one blocked edge.

=== STAGE 5: 结果回写 ===
Sub-chips "完成回执" / "阶段复盘卡片" / "保留 resultCardId 链路"; a tiny monochrome
receipt-plus-card thumbnail.

=== STAGE 6: 状态更新 ===
Sub-chips "不可变快照" / "新鲜度 FRESH / STALE / FAILED" / "变化简报"; a tiny monochrome
snapshot-stack thumbnail with a small clock glyph.

=== CONNECTIONS ===
Thick Cyan #0E7490 arrows Stage 1→2→3→4→5→6, then a dashed Deep Navy arrow from Stage 6 back
to Stage 1 labelled "下一次判断以新证据为输入"; a thin grey arrow from Stage 5 into Stage 6
labelled "刷新失败保留旧快照"; a dashed grey branch from Stage 3 "稍后 / 忽略" leading to a
small side box "仅记录反馈，不写入业务事实"; a thin arrow from Stage 4 back to Stage 2
labelled "缺证据则补记录".

=== STYLE SPECIFICATIONS ===
Two hues only: Deep Navy #142338 for structure and Cyan #0E7490 for the accepted path;
minimal Amber #D97706 for rate-limit markers and one Vermillion #DC2626 marker only if a
blocked dependency edge is drawn. White module fills, 1px borders, faint grey #F4F7FA stage
panels, no gradient, no shadow, no 3D, no illustration of people or devices beyond small
monochrome UI thumbnails. Body text #142338, secondary #788C9E, arrows #475B70. At least
70 percent white canvas; fully legible in black-and-white print; sharp Chinese glyphs; no
watermark, no logo.
```

中文标注版：

> 画一张顺时针六阶段闭环流程图，16:9，扁平矢量，简体中文短标签，每个阶段框内必须有 2–3 个子标签和 1 个灰度缩略图，禁止空框。
> ① 触发检测（截止逼近 / 风险未处理 / 项目停滞 / 材料缺口 + 琥珀色小标记“每日预算 3 次 · 免打扰 23:00–08:00”）；② 证据组织（引用卡片 / 附件修订 / 触发原因）；③ 用户决策（接受·青边、稍后·灰边、忽略·灰边，上方手机通知条缩略图，脚注“系统不代替用户决定”）；④ 行动生成（标题优先级截止 / 依赖由当前数据评估 / 幂等去重键，含三节点依赖缩略图，其中一条边为阻塞）；⑤ 结果回写（完成回执 / 阶段复盘卡片 / 保留复盘链路）；⑥ 状态更新（不可变快照 / 新鲜度 FRESH·STALE·FAILED / 变化简报）。
> 连线：①→②→③→④→⑤→⑥ 用青色 #0E7490 粗箭头；⑥ 回到 ① 用藏青虚线箭头标注“下一次判断以新证据为输入”；③ 的“稍后 / 忽略”分一条灰虚线到侧栏“仅记录反馈，不写入业务事实”；④ 回到 ② 一条细箭头“缺证据则补记录”；⑤→⑥ 标注“刷新失败保留旧快照”。
> 配色只用藏青 #142338 + 青 #0E7490，琥珀仅用于限频标记；白底细边框、分区浅灰 #F4F7FA、无渐变无阴影、≥70% 留白、黑白打印可读。

必须出现：用户决策在“行动生成”之前、预算与免打扰标记、幂等与依赖评估、回写形成新证据、刷新失败保留旧快照。
禁止出现：把 Agent 画成自动执行（跳过用户确认）、写“自动完成”“自动同步日历”、出现负责人分配作为当前能力、彩色大色块。

---

## 图 6.4 — 宣传海报（Agent 方向鼓励项）

适用类型：poster
推荐尺寸：2:3 竖版（1024×1536）；如需展板可 3:4

```
A tall 2:3 academic-competition poster for an AI agent product, flat Swiss-grid poster design,
strictly two-color plus neutral palette, generous white space, strong typographic hierarchy,
no photographic imagery, no 3D rendering, no gradient mesh, no glassmorphism, no sci-fi
glow, no robot illustration.

=== HEADER BAND (top 18 percent, white) ===
Large Simplified Chinese title "忆程 ProjectMemo" in Deep Navy #142338, a compact abstract
two-stroke logotype mark in Navy and Cyan #0E7490 placed to its left (an interlocking
memory-loop glyph, geometric, no letterforms inside), beneath the title a single-line
Chinese subtitle "证据驱动的项目过程档案与决策手账" in Medium Grey #788C9E, and a thin
full-width grey hairline rule separating the band.

=== ROW 1: 问题（left third） ===
Three stacked short Chinese lines with tiny monochrome icons: "过程没有沉淀" /
"状态靠记忆判断" / "成果重复整理", each on its own white row card with a 1px grey border.

=== ROW 2: 闭环主视觉 (center, largest block, about 45 percent of poster height) ===
A six-node circular diagram drawn with thin Navy strokes and one Cyan highlighted arc:
节点标签依次为 "记录" "关联" "判断" "确认" "行动" "回写"; at the center of the ring a
two-line Chinese statement "每条建议都有出处" / "每次变更都可回退"; small monochrome
thumbnail cards float beside the 记录 and 回写 nodes (a text-fragment card, a receipt card).

=== ROW 3: 三栏要点 (bottom third, three equal columns, white cards, 1px Navy borders) ===
Column A title "鸿蒙端" with bullets "ArkTS / ArkUI" "手机·平板·2in1" "系统通知与深链"
"图片与 PDF 附件"; Column B title "可信记忆" with bullets "四态时态模型"
"逐句来源引用" "人工确认才生效"; Column C title "主动推进" with bullets "每日提醒预算"
"免打扰时段" "完成结果回写复盘".

=== FOOTER STRIP (bottom 8 percent, faint grey #F4F7FA) ===
One line of small Chinese text: "2026 中国高校计算机大赛—人工智能创意赛 · 鸿蒙赛道 · Agent 创新"
and "万卷 siu · 武汉大学"; a small grey note "本地 Demo 与模拟器演示数据"; no QR code,
no URL, no contact number.

=== STYLE SPECIFICATIONS ===
Deep Navy #142338 and Cyan #0E7490 are the only hues; everything else is white #FFFFFF,
faint grey #F4F7FA panels, and neutral text greys #142338 / #475B70 / #788C9E. No Amber, no
Vermillion, no rainbow, no gradient, no drop shadow, no bevel, no lens flare, no neon.
Modular 12-column grid, aligned baselines, consistent 1px hairlines, high contrast,
typographic rather than illustrative. All Chinese glyphs must be crisp, correctly stroked,
and free of invented characters; keep every Chinese string short (under 12 characters per
line) so it renders legibly. At least 60 percent white space. Aspect ratio 2:3.
```

中文标注版：

> 做一张 2:3 竖版竞赛海报，瑞士网格排版，只用深藏青 #142338 与青 #0E7490 两个颜色加中性灰白，禁止照片、3D、渐变、玻璃拟态、霓虹光效和机器人插画。
> 顶部 18%：大号中文标题“忆程 ProjectMemo”，左侧一个由两笔构成的几何记忆环标志（藏青 + 青，标志内不含字母），下方一行灰字副标题“证据驱动的项目过程档案与决策手账”，再一条通栏细灰分割线。
> 第一行（左三分之一）：三行短句配极小灰度图标——“过程没有沉淀”“状态靠记忆判断”“成果重复整理”，各自白底 1px 灰边行卡。
> 第二行（中部主视觉，约占高 45%）：六个节点的圆环图，细藏青描边 + 一段青色高亮弧，节点标签“记录 / 关联 / 判断 / 确认 / 行动 / 回写”，环心两行中文“每条建议都有出处”“每次变更都可回退”，记录与回写节点旁各漂浮一张灰度小卡（文本碎片卡、回执卡）。
> 第三行：三等宽白卡栏，藏青 1px 边框——「鸿蒙端」：ArkTS / ArkUI、手机·平板·2in1、系统通知与深链、图片与 PDF 附件；「可信记忆」：四态时态模型、逐句来源引用、人工确认才生效；「主动推进」：每日提醒预算、免打扰时段、完成结果回写复盘。
> 底部 8%（浅灰 #F4F7FA 条）：小字“2026 中国高校计算机大赛—人工智能创意赛 · 鸿蒙赛道 · Agent 创新”与“万卷 siu · 武汉大学”，并一行灰注“本地 Demo 与模拟器演示数据”。
> 约束：每行中文不超过 12 字以保证渲染清晰；无二维码、无链接、无电话号码；≥60% 留白；线条统一 1px；黑白打印仍完整可读。

必须出现：作品名、副标题、六节点闭环、三栏要点、赛道与方向标注。
禁止出现：虚构获奖/用户数/下载量/效率百分比、华为或小艺官方 logo、真实截图拼贴、联系方式与二维码、把产品画成通用聊天机器人。

---

## 生成后核对清单（每张图都过一遍）

- [ ] 中文标签逐字核对，无错字、无杜撰字符、无串行重叠。
- [ ] 图内不含：真实域名/IP、账号、密钥、个人数据库路径、签名证书信息。
- [ ] 状态与口径与草案 §4.4 功能边界一致：小艺为“待验证”，端侧为“需后端”，通知为“已发布不等于已看到”。
- [ ] 图号、图题、图注与正文连续编号一致，图注保留“本地 Demo / 模拟器”字样。
- [ ] 黑白打印测试：仅靠边框虚实、线宽和文字即可区分层级。
- [ ] 导出为 PNG（≥2000px 长边）或 SVG，文件名 `图4_复赛主线闭环.png` 形式，放入 `docs/competition/assets/`。
- [ ] 若与 Mermaid 版并存，正式提交稿只保留一套，避免同一逻辑出现两种画法。
