# E4 收尾回执：F1 可行性判定与编辑界面

日期：2026-09-12。起点 HEAD `e613545`。

## 改动

1. **判定语义**（上一轮已完成严格映射）：仅 SUPPORTED=MET；SUPERSEDED/REVOKED=UNMET；INSUFFICIENT/PENDING/CONFLICT/缺状态=UNKNOWN；界面写明"记录卡依赖表示依赖该结论成立，仅存在记录并不代表前提满足"。
2. **API 扩展**：`removeRequirementIds` 删除依赖；估时校验前移——非法估时与新增依赖同请求时整体验证失败，不留部分依赖（测试锁定）；增删/估时在服务端顺序执行并重评。
3. **ActionFeasibilityEditor.ets**：依赖列表（状态+删除）、三类候选选择器（行动/记录卡/交付物，行动/交付物过滤非自己/非完成）、硬依赖/参考切换、估时设置与清空；保存后服务端重评返回，编辑器即时刷新依赖与状态。错误（跨项目/环/非法估时）就地显示。
4. **先解决依赖防重复**：创建前检查现有 TODO/DOING 是否已有"先解决依赖："任务，存在则提示不重复创建。

## 命令与退出码

- `npm.cmd exec tsc -- --noEmit` → 0
- `npm.cmd test`（独立库，含可行性 8 用例：非法估时回滚、删除依赖、仅人工确认 UNKNOWN、撤销 UNMET）→ 全部通过，0
- `npm.cmd run harmony:test` → BUILD SUCCESSFUL，27/27

## 状态

| 项 | 代码验收 | 设备验收 | 总状态 |
|---|---|---|---|
| F1 | PASS | 尚未执行 | IN_PROGRESS |

## 剩余限制

- 编辑器为全屏模态（非 bindSheet），因宿主可行性弹窗为自定义模态，需真机确认层级与键盘行为。
- 候选列表一次全量拉取（无分页/搜索），大项目上需后续优化。
